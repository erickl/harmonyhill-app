import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.linnieklaes.harmonyhill.manager',
  appName: 'Harmony Hill Manager',
  webDir: 'build'
};

export default config;
